{
  "name": "@reapp/consumer-agent",
  "version": "0.0.0",
  "private": true,
  "description": "Reference ResearchAgent: signs/registers a mandate and pays via @reapp/sdk agent.fetch.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "tsx src/research-agent.ts"
  },
  "dependencies": {
    "@reapp/sdk": "*"
  }
}
